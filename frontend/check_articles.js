const fs = require('fs'); const path = require('path'); const guidesDir = './src/content/guides'; const mdFiles = fs.readdirSync(guidesDir).filter(f => f.endsWith('.md')); mdFiles.forEach(file => { const content = fs.readFileSync(path.join(guidesDir, file), 'utf-8'); const imgMatch = content.match(/^image:\s*"?\/images\/([^"]+)"?/m); const titleMatch = content.match(/^title:\s*"?([^"]+)"?/m); if(imgMatch && titleMatch && !imgMatch[1].match(/\d{13}/)) { console.log(file + ' | ' + titleMatch[1] + ' | ' + imgMatch[1]); } });
